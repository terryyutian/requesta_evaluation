version: 1.0
runtime: python3
build:
  command: pip install --no-cache-dir -r backend/requirements.txt
run:
  runtime-version: 3.11
  command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
