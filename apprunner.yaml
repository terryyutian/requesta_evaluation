version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "No-op build phase for Python 3.11"

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install --no-cache-dir -r backend/requirements.txt
    - mkdir -p backend/data
  command: python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env: PORT
  env:
    - name: ENV
      value: production
    - name: DEBUG
      value: "false"
